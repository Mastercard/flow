package com.mastercard.test.flow.doc.mask;

import com.mastercard.test.flow.Actor;

/**
 * Trivial example of system actor definition.
 * <p>
 * This class isn't actually used for anything, but bits of it will be extracted
 * from here and inserted into the main readme. Having it in an active
 * compilation path means we can be sure that our example code remains valid in
 * the face of API changes
 * </p>
 */
@SuppressWarnings("javadoc")
enum Actors implements Actor {
	AVA, BEN, DIE
}
