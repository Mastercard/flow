package com.mastercard.test.flow.report.detail;

/**
 * Exercises the context tab on a file-served report
 */
class ServedContextTest extends AbstractContextTest {

	/***/
	ServedContextTest() {
		super( report.url() );
	}
}
