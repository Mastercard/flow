package com.mastercard.test.flow.report.detail;

/**
 * Exercises the residue tab on a file-served report
 */
class ServedResidueTest extends AbstractResidueTest {

	/***/
	ServedResidueTest() {
		super( report.url() );
	}
}
